#include <stdio.h>

int main() {
    float sales[7];
    float total = 0;
    int i;

    for(i = 0; i < 7; i++) {
        printf("Enter sales for day %d: ", i + 1);
        scanf("%f", &sales[i]);
        total += sales[i];
    }

    printf("\nTotal weekly sales = %.2f", total);

    return 0;
}
