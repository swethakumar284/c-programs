#include <stdio.h>

int main() {
    char str[100];
    int count = 0, i = 0;

    printf("Enter a string: ");
    fgets(str,100,stdin);
    while(str[i] != '\0') {
        count++;
        i++;
    }

    printf("Number of characters = %d", count);

    return 0;
}
