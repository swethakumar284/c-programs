#include <stdio.h>

struct Student
{
    int id;
    char name[20];
};

int main()
{
    struct Student s[5];
    int n;

    printf("Enter number of records: ");
    scanf("%d", &n);

    printf("Total number of records = %d\n", n);

    return 0;
}